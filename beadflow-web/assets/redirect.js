(() => { const params=new URLSearchParams(location.search); const explicit=params.get('lang'); const stored=localStorage.getItem('beadflow-language'); const detected=(navigator.languages?.[0]||navigator.language||'en').toLowerCase().startsWith('zh')?'zh':'en'; const lang=explicit==='zh'||explicit==='en'?explicit:(stored||detected); localStorage.setItem('beadflow-language',lang); const page=document.documentElement.dataset.page||'index'; location.replace(`./${lang}/${page}.html`); })();